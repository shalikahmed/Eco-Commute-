{
    "build": {
      "env": {
        "NPM_FLAGS": "--legacy-peer-deps"
      },
      "commands": {
        "install": "npm install $NPM_FLAGS",
        "build": "npm run build"
      }
    }
  }
  